{
	"extends": "./tsconfig.json", // 拓展 tsconfig.json 的配置
	"compilerOptions": {
		"noEmit": false, // 允许生成文件
		"declaration": true, // 需要设置为 true 来支持类型
		"emitDeclarationOnly": true, // 只生成类型文件
		"outFile": "./dist/toolkit-x1.d.ts"
	},
	"files": ["src/index.ts"]
}
